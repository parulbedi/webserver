name: MN 🐍 installing python
on: workflow_call
jobs:
  installing_python:
     runs-on: [self-hosted, managed-node]
     steps:
     - name: installing python via github actions
       run: |
        hostname
        sudo apt-get update
        sudo apt install python3 -y
