name: installing ansible via Github Actions

on: push
runs-on: self-hosted
jobs:
  installing_ansible:
    steps:
    - name: installing ansible via github actions
      run: |
        ls -al
        sudo apt-get update
        sudo apt-get upgrade
        sudo apt install ansible
